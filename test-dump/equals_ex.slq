n := 3;

def func(const p:B, q:uint[n]){
    if p {
        q = 3:uint[n];
    }
    return q;
}

def main(){
    q := 1 :uint[n];
    p := H(0:B);
    // if p {
    //     q = 3:uint[n];
    // }
    q := func(p,q);
    measure(p);
    return q;
}